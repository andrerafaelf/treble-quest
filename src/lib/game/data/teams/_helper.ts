import type { PlayerSeason, Position } from '$lib/game/types';

export const p = (
  id: string,
  name: string,
  club: string,
  season: string,
  nationality: string,
  positions: Position[],
  role: string,
  overall: number,
  attack: number,
  control: number,
  defence: number,
  clutch: number,
  consistency: number,
  chaos: number,
  rarity: PlayerSeason['rarity'],
): PlayerSeason => ({
  id,
  name,
  club,
  season,
  nationality,
  positions,
  role,
  overall,
  attack,
  control,
  defence,
  clutch,
  consistency,
  chaos,
  rarity,
});
